package ${project.groupId}.${project.name}.server.persistence.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import ${project.groupId}.${project.name}.server.persistence.po.${poName};

public interface ${mapperName} extends BaseMapper<${mapperName}> {

}